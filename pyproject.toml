[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[project]
requires-python = ">=3.9,<3.11"
name = "airflow"
version = "1.0"
description = "My Data Warehouse sandbox"
# authors = ["Max Shevtsov <max.shvtsv@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
pandas = "^2.3.1"
numpy = "^1.24"
pyspark = "^4.0.0"
requests = "^2.32.4"
apache-airflow = "^3.0.3"

[tool.poetry.group.dev.dependencies]
pytest = "*"